- **Hint/miss bottom sheet** (rounded-top panel pinned to the bottom of the screen, with a heading, thumbs feedback icons, body text, and an action pair) — needed by Result & recovery's Partial and Fail states. Built inline in `app/recall/result/page.tsx`, not yet promoted to a Storybook component.
- **Pass badge** ("✓ Correct" pill bound to `feedback.success.bold`) — needed by Result & recovery's Pass state. Chips' own `Green`/active color binds `accent.green.bold` instead (same hex, different semantic token — same distinction `ResultRow` already special-cased), so Chips wasn't reused as-is; built inline in `app/recall/result/page.tsx`.
- **ButtonGroup has no per-button `state` prop** — Answer's text mode can't visually disable "Send" when the field is empty (guards in the click handler instead, per design-system.md rule 9: never fake a state by hand). Needed by `app/recall/answer/page.tsx`.
