// 390px is the fixed frame width from sprint-context.md / design-system.md
// rule 10 -- a product layout constant, not a design token (there is no
// viewport-width category in tokens/tokens.json).
export const SCREEN_MAX_WIDTH = 390;
